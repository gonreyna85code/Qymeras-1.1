# ESP-IDF partition table for Qymera (4 MB flash)
#
# OTA is intentionally REMOVED from the qymera-IDF branch (no ota_0/ota_1/
# otadata). A single `factory` app partition is used, matching the UDP-only,
# no-OTA runtime. Storage lives in NVS (credentials, settings, calibration,
# rules) backed by the historical 4 KiB EEPROM image stored as one blob.
#
# Name,    Type, SubType, Offset,   Size
nvs,       data, nvs,     0x9000,   0x6000,
phy_init,  data, phy,     0xf000,   0x1000,
factory,   app,  factory, 0x10000,  0x390000,
